export default class MyComponent {

  static someWords() {
  
    return "These are words from a javascript class. They could come from some rest call to a webservice that talks to a database";
  }
}
